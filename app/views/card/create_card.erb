	<% cards.each do |card| %>
<%= erb :"_form", :locals => {card: card, card_id: card.id } %>
<% end %>
<% if add_card %>
<%= erb :"_form", :locals => {card: Card.new, card_id: Card.maximum(:id).next } %>
<% end %>
  <input type="submit" value="Add Card"/>